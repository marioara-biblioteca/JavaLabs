import clasa.*;
import task1.*;

import java.util.ArrayList;

public class Main {
    public static void main(String[] args) {
    /*
        //task3
        Car ford=new Car(12000,2010, Car.Type.FORD);
        Car jeep=new Car(15000,2000,Car.Type.JEEP);
        Car tesla=new Car(1000000,2020,Car.Type.TESLA);
        Dealership d=new Dealership();
        d.getFinalPrice(ford);
        d.getFinalPrice(jeep);
        d.getFinalPrice(tesla);

        //task4
        Offer o=new Offer(){
            public int getDiscount(Car car){
                return 200;
            }
        };
        d.negotiate(jeep,o);
        //task5
        Car jeep2=new Car(10000,1999,Car.Type.JEEP);
        Car tesla2=new Car(150000,2022, Car.Type.TESLA);
        ArrayList<Car>cars=new ArrayList<>();
        cars.add(jeep2);cars.add(jeep);cars.add(tesla2);cars.add(tesla);cars.add(ford);
        System.out.println(cars.size()+"    "+cars);
        cars.removeIf(car->car.getPrice()>13000);
        System.out.println(cars.size()+"    "+cars);*/
    }
}